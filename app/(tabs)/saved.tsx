import React from "react";
import { Text, View } from "react-native";

const Saved = () => {
  return (
    <View className="flex-1 items-center justify-center bg-white">
      <Text>Saved Section of the App</Text>
    </View>
  );
};

export default Saved